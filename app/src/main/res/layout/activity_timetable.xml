<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Manage Timetable"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif"
            android:layout_marginBottom="16dp"
            android:textColor="#FFFFFF" />

        <!-- Button to upload Excel file -->
        <Button
            android:id="@+id/uploadExcelButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload Excel File"
            android:textSize="16sp"
            android:background="@drawable/gradient_background"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Button to upload PDF file -->
        <Button
            android:id="@+id/uploadPDFButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload PDF File"
            android:textSize="16sp"
            android:background="@drawable/button_background"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Section to display timetable data -->
        <TextView
            android:id="@+id/timetableHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Uploaded Timetable"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="8dp"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/timetableDataView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No timetable uploaded yet."
            android:textSize="16sp"
            android:background="@drawable/timetable_data_background"
            android:padding="12dp"
            android:textColor="#FFFFFF" />
    </LinearLayout>
</ScrollView>
